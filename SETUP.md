## ⚙️ セットアップ (Setup)

### 👥 Humans (人間用)

> [!TIP]
> **人間は設定を打ち間違えることがあります。** このプロジェクトのインストールと設定は、AIエージェント（Claude Code / Gemini CLI / Cursor / OpenCode 等）に丸投げすることを強く推奨します。以下のプロンプトをコピーしてチャットセッションに貼り付けてください。

```text
Follow the Agent Setup Protocol in this document to install and configure dotfiles-term:
./SETUP.md
```

---

<details>
<summary>🤖 AIエージェント向け自動セットアッププロトコル（詳細を表示）</summary>

### 🤖 エージェント実行制約規約 (Agent Execution Block)

> [!CAUTION]
> **AIエージェントへの強制命令 (CRITICAL RUNTIME CONSTRAINT):**
> あなたが AI エージェントである場合、いかなる副作用（コマンド実行、インストール、ファイルの作成・修正）を伴うアクションを開始する前に、必ず以下の **Agent Setup Protocol** を最初から愚直に遵守してください。
> ステップを要約・スキップ・推測して自動進行することは厳格に禁止されます。

> [!IMPORTANT]
> **🚨 ユーザー確認ツールの使用強制 (STRICT ASK CONSTRAINT):**
> 各フェーズの **`BLOCKING STEP`** を実行する際は、**絶対にチャットテキスト上の対話だけで自己完結して進めてはいけません。**
> あなたが `ask_question` や `ask_permission`、またはそれに類するユーザー承認確認用ツールを装備している場合、それらを明示的かつ強制的に呼び出して、ユーザーがUI上で承認・選択するプロセスを確実に踏んでください。ツール経由での明示的な承認が得られるまで、次のステップへ進むことは厳格に禁止されます。

<agent-rules>

#### ⚠️ 勝手な解釈・スキップ防止ルール（Anti-Guessing Rules）
1. **構成の自動仮定禁止**:
   プロジェクト内の環境変数や接続先について、「デフォルトはこれだろう」とエージェント自身で勝手に判断して進めてはいけません。必ず Phase 2 のインタビューを完全に完了させ、その回答にのみ従ってください。
2. **書込後の実態確認の義務化 (Self-Verification)**: 
   設定ファイル（シンボリックリンクの作成等）をした後は、**必ず直後に `ls -l` や `readlink` 等の読込ツールを使用して実態を確認し、実際に期待するリンク先や設定が適用されていることを目視（ログ上での解析）で検証してください。**
3. **実態証明の出力**:
   すべての変更を行った後、`ls -la <対象パス>` などのコマンドを実行し、ファイル/リンクの存在を明示的に出力した上で、完了報告を行ってください。実際に作成・変更されたことをコマンド出力で証明してから報告してください。

#### 💻 ターゲット環境（OS）の自動適応ルール（OS Adaptation Rule）
1. **OS の能動的判定**: ファイルやスクリプトを作成・実行する前に、必ず `uname` や環境変数等を確認し、現在の実行環境（Linux, macOS）を特定してください。
2. **実行環境に合わせた設定の上書き**: Linux環境では `dconf` による Tilix 設定のロードを試みますが、macOS等の非Linux環境ではスキップすることをユーザーに明示してください。

</agent-rules>

<setup-phases>

#### 🟦 Phase 1: 目的の確定と実行モードの選択 (BLOCKING STEP)
いかなる変更・インストールを伴うツールを呼び出す前に、まずユーザーに以下の質問を提示し、セットアップ目的および **実行モード** を **確定 (Lock-in)** させてください。

> 1. **実行モードの選択**: `本番モード`（実際にシンボリックリンクの作成、設定のバックアップ、dconfのロードを行う）または `デバッグモード（Dry-run）`（変更を行わず、実行される予定のコマンドを表示するだけ）のどちらで実行しますか？

> [!IMPORTANT]
> **デバッグモード（Dry-run）の厳格な非破壊ルール:**
> ユーザーが `デバッグモード` を選択した場合、AIエージェントであるあなたは **絶対にファイルを一切変更（作成、更新、削除）してはならず、システムへの変更コマンドを実行してもいけません。** 代わりに、実行されるべき `ln -sfn` コマンドやバックアップのロジックを画面上に提示し、「もし本番実行された場合、どのような変更が行われるか」をシミュレーションして解説するだけに留めてください。

#### 🟦 Phase 2: 構成情報の確認と一括ロックイン (BLOCKING STEP)
いかなるファイル操作ツールを呼び出すよりも前に、必ず **`ask_user` 等のユーザー確認ツールを使用して、以下の確認項目を一括で提示し、回答を完全に確定させてください。** 質問を小出しにしたり、勝手に省略してはいけません。

> 1. **セットアップ対象の選択 (複数回答可)**:
>    - [ ] WezTerm (~/.wezterm.lua)
>    - [ ] Ghostty (~/.config/ghostty/config)
>    - [ ] Tilix (dconf load /com/gexperts/Tilix/)
>    - [ ] tmux (~/.tmux.conf)
> 2. **依存関係の確認**: `dotfiles-core` (特に `../common-mk/`) が存在し、このリポジトリがコンポーネントとして適切に配置されていることを確認しましたか？
> 3. **既存設定のバックアップ**: 実ファイルが存在する場合、自動的に `.bak.<timestamp>` として退避されますが、これを許可しますか？
> 4. **dconf の実行許可 (Linuxのみ)**: Tilix の設定をシステムに書き込むために `dconf load` を実行してもよろしいですか？

#### 🟦 Phase 3: 環境構築と機密情報の分離収集 (BLOCKING STEP)
1. **依存関係の解決**:
   `make help` を実行し、`common-mk` への参照が正しく動作していることを確認します。

2. **設定の適用（本番モード用）**:
   上記で確定した構成に基づき、`make setup` を実行します。このコマンドは以下の副作用を伴います：
   - `ln -sfn "$(CURDIR)/wezterm.lua" "$(HOME)/.wezterm.lua"`
   - `mkdir -p "$(HOME)/.config/ghostty"` へのディレクトリ作成とリンク
   - `dconf load /com/gexperts/Tilix/ < tilix/tilix.dconf` (Linuxのみ)
   - `ln -sfn "$(CURDIR)/tmux/tmux.conf" "$(HOME)/.tmux.conf"`
   変更後は、**必ず `ls -l` を用いて、正しくシンボリックリンクが作成されたことを自己検証し、実在証明を出力してください。**
   *(※デバッグモードの場合は、実際のリンク作成は行わず、シミュレーション用のコマンドを画面上に提示するだけに留めます)*

3. **機密情報の分離収集ルール**:
   本プロジェクトには API キー等の機密情報は含まれませんが、ユーザー独自の `ghostty.conf` や `wezterm.lua` に秘密の設定を書き込みたい場合は、リンク先のファイルではなく、各ツールの include 機能を利用した別ファイルを個別に管理するようユーザーに案内してください。

4. **入力完了の待機と実態検証 (Strictest Verification)**:
   ユーザーから「依存関係の準備ができた」という報告があるまで待機してください。実行後、エージェントは**直ちに `ls -l` を実行して各リンク先を再スキャンしてください。** 実際にリンクが作成されており、不正なパスでないことをログ上で厳格に検証してください。

#### 🟦 Phase 4: 接続確認と最終検証
1. **検証コマンドの実行（必須）**:
   シンボリックリンクの配置および実態確認が成功したことを確認後、実際に以下の検証コマンドを実行し、設定が正しく参照されていることを確認してください。

```bash
# シンボリックリンクの整合性チェック
[ -L ~/.wezterm.lua ] && [ -L ~/.config/ghostty/config ] && [ -L ~/.tmux.conf ] && echo "Links verified."

# Tilix 設定の反映確認 (Linuxのみ)
if command -v dconf >/dev/null 2>&1; then dconf dump /com/gexperts/Tilix/ | head -n 20; fi

# Makefile によるテストターゲットの実行
make test
```

2. **重複記載の確認とプロトコル追記 (DUPLICATION CHECK CONSTRAINT)**:
   セットアップが正常に完了したら、プロジェクト内のエージェント指示ファイル（`AGENTS.md`）を読み込み、すでに本プロトコルと同等の指示が記載されていないか確認してください。
   記載がない場合のみ、ユーザーの明示的な承認（`ask_permission` 等）を得てから、必要なプロンプトをファイルの末尾に追記してください。

3. **完了報告**:
   すべての検証がパスしたことを確認し、ユーザーにセットアップ完了を報告してください。

</setup-phases>
</details>
